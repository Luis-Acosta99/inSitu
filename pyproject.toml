[project]
name = "insitu"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Luis-Acosta99", email = "luis.fer.acosta99@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.141.1",
    "pandas>=3.0.5",
    "playwright>=1.62.0",
    "psycopg2-binary>=2.9.12",
    "uvicorn>=0.52.0",
]

[project.scripts]
insitu = "insitu:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
